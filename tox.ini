[tox]
envlist = py27,py35,lint

[testenv]
deps = -r{toxinidir}/requirements.txt
commands =
    nosetests -svx konfig/tests --with-coverage --cover-package=konfig

[testenv:lint]
commands =
    pylint --rcfile pylintrc --disable=W0511 --ignore tests konfig

